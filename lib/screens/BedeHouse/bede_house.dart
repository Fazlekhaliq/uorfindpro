
import 'package:flutter/material.dart';

class BedeHouseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bede House'),
      ),
      body: Center(
        child: Text('Bede House Screen'),
      ),
    );
  }
}